import styled from 'styled-components';
import tw from 'twin.macro';

export const Container = styled.div`
  ${tw`md:flex min-h-screen`}
`;

export const Wrapper = styled.div`
  ${tw`pt-14 px-8 pb-6 w-full md:w-4/5 `}
`;
